# name: ci
# on:
#   push:
#     branches:
#       - latest
#       - beta
# permissions:
#   contents: write
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v4
#         with:
#           python-version: 3.x
#       - run: echo "cache_id=$(date --utc '+%V')" >> $GITHUB_ENV
#       - uses: actions/cache@v3
#         with:
#           key: mkdocs-material-${{ env.cache_id }}
#           path: .cache
#           restore-keys: |
#             mkdocs-material-
#       - run: pip install "mkdocs-material mike"
#       - run: mike deploy -

name: ci
on:
  push:
    branches:
      - latest
      - beta
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hi=$(Hello world!)" >> $GITHUB_ENV
      - uses: actions/cache@v3
        with:
          key: test-${{ env.hi }}
          path: .cache
          restore-keys: |
            test-
